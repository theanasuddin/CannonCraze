// Top-level build configuration for the Cannon Craze Android app.

plugins {
    id 'com.android.application' version '8.6.1' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.layout.buildDirectory
}
